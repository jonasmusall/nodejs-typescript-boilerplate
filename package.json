{
  "name": "nodejs-typescript-boilerplate",
  "version": "0.0.0",
  "description": "Minimalistic template for Node.js apps build with TypeScript and VS Code. No tests or linters.",
  "author": "Jonas Musall",
  "license": "MIT",
  "engines": {
    "node": "^17.9.0"
  },
  "scripts": {
    "start": "node src/main.js",
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -w -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^17.0.23",
    "typescript": "^4.6.3"
  }
}
